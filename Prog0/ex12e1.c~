#include <stdio.h>

int isprime(int);
int n;

int main() {
  int flg=0;
  while(1){
    if(isprime()==1){
      printf("%d",isprime(n));
      flg++;
      if(flg%10==0){
	printf("\n");
      }
    }
  }
  return 0;
}

int isprime(int x){
  int box[999];
  for(int i=0;i<999;i++){
    box[i]=i+2;
    if(box[i]%(i+2)
  }
  
